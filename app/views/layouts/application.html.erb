<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "CitizenII" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>
  <%= yield :head %>
  
  </head>
  <body>
  	
 
  	
 <div id = 'pagewrap'>
 	<% if user_signed_in? && current_user.admin? %>
    	<%= render 'shared/admin'%>
    <%end%>
 	
 	
	<div id = 'hmenu'>
		<ul>
			<li><%= link_to "Home", root_path  %></li>
 			<li><%= link_to "Lunch Menu", '/menus/1'%></li>	
			<li><%= link_to "Box Lunch Menu", '/boxmenus/1'  %></li>
			<li><%= link_to "Map", map_path %></li>
			<li><%= link_to "Gallery", '/galleries/5'  %></li>
			<li><%= link_to "About Citizen"  %></li>
			
			
		</ul> 
 	 
 		<div id = "shift">
    			<ul >
	                 <% if user_signed_in? %>
	          			<li class="nav_text_sm"><%= current_user.email %>  </li>  |         			 
	          			<li class="nav_text_sm"><%= link_to "Sign out", destroy_user_session_path, :method => :delete %></li>
	        		<% else %>
	          			<li class="nav_text_sm"><%= link_to "SIGN UP", new_user_registration_path %></li> | 
	          			<li class="nav_text_sm">  <%= link_to "SIGN IN", new_user_session_path %></li>
	        		<% end %>
  				 </ul>       
		</div>
	</div>	
	
	
     	<div id = 'clear'></div>	

	
      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      <% end %>
 

		<div class="main">
			<%= yield %>
		</div>
 

</div>
    <div class="bottombar" >

       
        	&copy; Citizen MMXII  | 909 E. Main St, Richmond VA |  (804) 780-9038  |  Hours: Monday - Friday 7am - 3pm

	 

     </div>

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
               <%= javascript_include_tag "application", 'jquery-ui-timepicker-addon'%>
<%= yield :javascript%>
<%= yield :scripts %>
  </body>
</html>
